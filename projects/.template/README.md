# Project Template

Copy this directory structure to create a new project.

## Quick Start

1. Copy this template:
```bash
cp -r projects/.template projects/my-project
```

2. Edit `config.json` with your project details

3. Create plans in `plans/` directory

4. Let Powerlevel handle the rest!
